let Observable;_08c‍.w("rxjs",[["Observable",["Observable"],function(v){Observable=v}]]);

let newArray = [];
let Obs = [44,8,5,27,7,1,0,7,6,3,4,10,7,1,9,5,9,7];

let observe = new Observable((subscriber) => {
    let i = 0;

    for(i; i<Obs.length;i++) {
        timeOut(Obs[i], i, subscriber);
    }
    timeOut('Wow', i++ , subscriber)
})

function timeOut(value, i, subscriber) {
    setTimeout(() => {
        // console.log(i)
        subscriber.next(value)
    }, i *400);
}

let sev_occur = 0;
observe.subscribe(value => {
    _08c‍.g.console.log(value)
    if(value == 7) {
        sev_occur++;
    }
    if(value == 'wow') {
        _08c‍.g.console.log('new array ' + newList)
        _08c‍.g.console.log('seven occured ' + sev_occur)
        return
    }
    newArray.push(value)
    
})